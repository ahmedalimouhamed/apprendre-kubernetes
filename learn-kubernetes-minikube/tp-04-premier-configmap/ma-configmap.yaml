apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  application.properties: |
    app.name=MonApp
    app.version=1.0
    environment=development
  message: "Bonjour depuis kubernetes!"